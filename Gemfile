# frozen_string_literal: true

# This tells bundler the default place to get ruby gems from.
source 'https://rubygems.org'

gem 'Nessus6', '>= 0.3.4', '< 1.0'
gem 'nessus-tasks'
gem 'pipeline-tasks'
gem 'stig-processing'
